<section class="hero">
  <div class="image"></div>
  <div class="overlay"></div>
  <div class="content">
    <p class="eyebrow">Commercial photography & film</p>
    <h1>Visual stories for organisations that need to explain, inspire and connect.</h1>
    <p>I create photography and film that helps businesses communicate what they do, who they are, and why they matter.</p>
    <a href="/contact">Start a conversation</a>
  </div>
</section>

<style>
.hero {
  min-height: 92vh;
  display: flex;
  align-items: flex-end;
  position: relative;
  padding: 4rem 3rem 7rem;
  background: #111;
  color: white;
  overflow: hidden;
}

.image {
  position: absolute;
  inset: 0;
  background-image: url('/images/portfolio/eurofighters.jpg');
  background-size: contain;
  background-repeat: no-repeat;
  background-position: center;
}

.overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(90deg, rgba(0,0,0,.7), rgba(0,0,0,.25));
}

.content {
  position: relative;
  z-index: 1;
  max-width: 950px;
}

.eyebrow {
  text-transform: uppercase;
  letter-spacing: .15em;
  font-size: .8rem;
  margin-bottom: 1.5rem;
}

h1 {
  font-size: clamp(2.8rem, 6vw, 5.5rem);
  margin: 0 0 1.5rem;
}

p {
  font-size: 1.25rem;
  max-width: 700px;
}

a {
  display: inline-block;
  margin-top: 2rem;
  padding: 1rem 2rem;
  border: 1px solid white;
  transition: .3s ease;
}

a:hover {
  background: white;
  color: black;
}
</style>
