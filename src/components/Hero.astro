<section class="hero">
  <img class="image" src="/images/portfolio/eurofighters.jpg" alt="Eurofighters" />
  <div class="overlay"></div>
  <div class="content">
    <p class="eyebrow">Commercial photography & film</p>
    <h1>Helping organisations tell their story with clarity, credibility and a bit of humanity.</h1>
    <p>I create photography and film for businesses that need to explain complex ideas, showcase expertise and connect with the people behind the brand.</p>
    <a href="/contact">Start a conversation</a>
  </div>
</section>

<style>
.hero {
  position: relative;
  overflow: hidden;
  min-height: 85vh;
  background: #111;
  color: white;
  display: flex;
  align-items: flex-end;
}
.image {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
}
.overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(90deg, rgba(0,0,0,.78), rgba(0,0,0,.2));
}
.content {
  position: relative;
  z-index: 1;
  max-width: 950px;
  padding: 0 3rem 5rem;
}
.eyebrow {
  text-transform: uppercase;
  letter-spacing: .15em;
  font-size: .8rem;
  margin-bottom: 1.5rem;
}
h1 {
  font-size: clamp(2.8rem, 5vw, 5rem);
  line-height: 1.05;
  margin: 0 0 1.5rem;
}
p {
  font-size: 1.2rem;
  max-width: 700px;
}
a {
  display: inline-block;
  margin-top: 2rem;
  padding: 1rem 2rem;
  border: 1px solid white;
}
@media(max-width:700px) {
  .hero { min-height: 75vh; }
  .content { padding: 0 1.5rem 2.5rem; }
}
</style>
